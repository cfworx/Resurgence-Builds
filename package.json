{
  "name": "resurgence-builds",
  "type": "module",
  "version": "1.0.0",
  "description": "Independent fan resource for Tom Clancy's The Division Resurgence — builds, guides, tier lists, patch notes.",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/optimize-images.mjs && astro build && node scripts/inject-lastmod.mjs && node scripts/inject-sitemap-images.mjs && npx pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "bump-patch": "node scripts/bump-patch.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6",
    "rehype-external-links": "^3.0.0",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.3.0",
    "rehype-img-size": "^1.0.1",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3"
  }
}
